# 金租分次放款 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把金租从「一次申请=一次放款」改为「一次批准→多次采购验收→多笔放款」，每笔放款生成独立还款计划。

**Architecture:** 新增 `leasing_disbursements` 放款记录子表（1 批准 → N 放款），`repayments` 挂 `disbursement_id`；`leasing_service.add_disbursement()` 取代单次 `disburse()`，每笔放款独立幂等、独立生成还款计划；采购验收作为放款前置（弱绑定）；批准金额为参考值不限额。

**Tech Stack:** FastAPI + SQLAlchemy + PostgreSQL（Alembic 迁移 + db/schema.sql 双写）、Vue3 + naive-ui、pytest + Playwright。

**Spec:** `docs/superpowers/specs/2026-08-19-金租分次放款-design.md`

**对 spec 的简化（已确认，减少无谓改动）：**
1. 工作流完成条件**不改**：`add_disbursement` 首笔放款仍置 `proc.status="已放款"`，既有 `completion_check(leasing_processes.status='已放款')` 自然触发。
2. `start_date` **不重命名列**：语义改为「预计放款日」，仅 UI 标签改「预计放款日」，后端列名保留（避免无谓迁移）。
3. **向后兼容**：旧 `disburse()` 服务函数 + `POST /processes/{id}/disburse` 端点**保留**（demo.py、既有 pytest/e2e 仍用）；前端 UI 切换到新的多笔放款路径，旧「放款操作」区**替换**为「放款记录」区。

---

## File Structure

- 新建 `backend/alembic/versions/0018_multi_disbursement.py` — 建 `leasing_disbursements` 表 + `repayments.disbursement_id` + 老数据回填
- 新建 `backend/app/tests/test_multi_disbursement.py` — 服务层 TDD
- 修改 `backend/app/models/leasing.py` — 加 `LeasingDisbursement` 模型
- 修改 `backend/app/models/repayment.py` — 加 `disbursement_id`
- 修改 `backend/app/models/__init__.py` — 导出 `LeasingDisbursement`
- 修改 `backend/db/schema.sql` — 双写新表 + 新列
- 修改 `backend/app/schemas/leasing.py` — 加 `DisbursementCreate` / `DisbursementOut`
- 修改 `backend/app/services/leasing_service.py` — 加 `add_disbursement()` + `list_disbursements()`
- 修改 `backend/app/api/v1/endpoints/leasing.py` — 加 `POST/GET /processes/{id}/disbursements`
- 修改 `frontend/src/views/LeasingView.vue` — 预计放款日 + 放款记录区 + 时间线按钮堆叠修复
- 修改 `e2e/tests/` — 新增分次放款 e2e

---

## Task 1: 模型层 —— LeasingDisbursement + repayments.disbursement_id

**Files:**
- Modify: `backend/app/models/leasing.py`
- Modify: `backend/app/models/repayment.py`
- Modify: `backend/app/models/__init__.py`

- [ ] **Step 1: 在 `leasing.py` 追加 LeasingDisbursement 模型**

在文件末尾（`LeasingNode` 类之后）追加：

```python
class LeasingDisbursement(UUIDPK, TimestampMixin, Base):
    """一笔放款记录（一次金租批准可多笔放款，每笔独立生成还款计划）。"""

    __tablename__ = "leasing_disbursements"

    process_id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), ForeignKey("leasing_processes.id"), nullable=False)
    amount: Mapped[Decimal] = mapped_column(Numeric(18, 2), nullable=False)
    disbursement_date: Mapped[date] = mapped_column(Date, nullable=False)  # 实际放款日（还款期从这天起算）
    note: Mapped[str | None] = mapped_column(Text, nullable=True)
    created_by: Mapped[uuid.UUID | None] = mapped_column(UUID(as_uuid=True), ForeignKey("users.id"), nullable=True)
```

- [ ] **Step 2: 在 `repayment.py` 的 `Repayment` 类加 `disbursement_id` 列**

在 `leasing_process_id` 行之后加：

```python
    disbursement_id: Mapped[uuid.UUID | None] = mapped_column(UUID(as_uuid=True), ForeignKey("leasing_disbursements.id"), nullable=True)
```

- [ ] **Step 3: 在 `models/__init__.py` 导出新模型**

读 `backend/app/models/__init__.py`，在 leasing 相关导出行（`"LeasingProcess", "LeasingNode"`）处追加 `"LeasingDisbursement"`。

- [ ] **Step 4: 提交**

```bash
git add backend/app/models/leasing.py backend/app/models/repayment.py backend/app/models/__init__.py
git commit -m "feat(leasing): add LeasingDisbursement model + repayments.disbursement_id"
```

---

## Task 2: 迁移 0018 + schema.sql 双写

**Files:**
- Create: `backend/alembic/versions/0018_multi_disbursement.py`
- Modify: `backend/db/schema.sql`

- [ ] **Step 1: 写迁移文件（建表 + 加列 + 老数据回填）**

```python
"""金租分次放款：leasing_disbursements + repayments.disbursement_id + 老数据回填。

Revision ID: 0018_multi_disbursement
Revises: 0017_return_orders
Create Date: 2026-08-19
"""
from alembic import op

revision = "0018_multi_disbursement"
down_revision = "0017_return_orders"
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.execute("""
        CREATE TABLE leasing_disbursements (
            id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
            process_id UUID NOT NULL REFERENCES leasing_processes(id),
            amount DECIMAL(18,2) NOT NULL CHECK (amount > 0),
            disbursement_date DATE NOT NULL,
            note TEXT,
            created_by UUID REFERENCES users(id),
            created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
            updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
            deleted_at TIMESTAMPTZ
        )
    """)
    op.execute("CREATE INDEX idx_disb_process ON leasing_disbursements(process_id) WHERE deleted_at IS NULL")
    op.execute("CREATE TRIGGER trg_disb_updated BEFORE UPDATE ON leasing_disbursements FOR EACH ROW EXECUTE FUNCTION set_updated_at()")

    op.execute("ALTER TABLE repayments ADD COLUMN IF NOT EXISTS disbursement_id UUID REFERENCES leasing_disbursements(id)")
    op.execute("CREATE INDEX IF NOT EXISTS idx_repay_disb ON repayments(disbursement_id) WHERE deleted_at IS NULL")

    # 老数据回填：已放款的单笔 process → 回填一条放款记录，并把其还款计划挂上去
    op.execute("""
        INSERT INTO leasing_disbursements (id, process_id, amount, disbursement_date, created_at, updated_at)
        SELECT gen_random_uuid(), id, actual_disbursement_amount, disbursement_date, now(), now()
        FROM leasing_processes
        WHERE plan_generated = TRUE AND actual_disbursement_amount IS NOT NULL AND disbursement_date IS NOT NULL
    """)
    op.execute("""
        UPDATE repayments r
        SET disbursement_id = d.id
        FROM leasing_disbursements d
        WHERE d.process_id = r.leasing_process_id
          AND r.disbursement_id IS NULL
    """)


def downgrade() -> None:
    op.execute("ALTER TABLE repayments DROP COLUMN IF EXISTS disbursement_id")
    op.execute("DROP TRIGGER IF EXISTS trg_disb_updated ON leasing_disbursements")
    op.execute("DROP TABLE IF EXISTS leasing_disbursements")
```

- [ ] **Step 2: schema.sql 双写（在 `backend/db/schema.sql` 末尾追加同样的建表/加列/索引）**

读 `backend/db/schema.sql`，找到 `repayments` 建表段，给 `repayments` 加 `disbursement_id UUID REFERENCES leasing_disbursements(id)`；并在文件末尾追加 `leasing_disbursements` 建表 + 索引 + 触发器（与迁移 SQL 逐字一致）。

- [ ] **Step 3: 验证迁移 up→down→up**

```bash
docker compose exec -T backend alembic upgrade head
docker compose exec -T backend alembic downgrade -1
docker compose exec -T backend alembic upgrade head
```
Expected: 三次都成功，无报错。

- [ ] **Step 4: 提交**

```bash
git add backend/alembic/versions/0018_multi_disbursement.py backend/db/schema.sql
git commit -m "feat(leasing): migration 0018 multi-disbursement table + backfill"
```

---

## Task 3: Schema（pydantic）—— DisbursementCreate / DisbursementOut

**Files:**
- Modify: `backend/app/schemas/leasing.py`

- [ ] **Step 1: 追加放款 schema**

在文件末尾追加：

```python
class DisbursementCreate(BaseModel):
    amount: Decimal = Field(gt=0)
    disbursement_date: date
    note: str | None = None


class DisbursementOut(BaseModel):
    id: UUID
    process_id: UUID
    amount: Decimal
    disbursement_date: date
    note: str | None = None
    created_at: datetime

    model_config = {"from_attributes": True}
```

（文件顶部需已 import `from datetime import date, datetime`——`date` 已 import，若 `datetime` 未 import 则补上。）

- [ ] **Step 2: 提交**

```bash
git add backend/app/schemas/leasing.py
git commit -m "feat(leasing): add DisbursementCreate/DisbursementOut schemas"
```

---

## Task 4: 服务层 —— add_disbursement + list_disbursements（TDD）

**Files:**
- Create: `backend/app/tests/test_multi_disbursement.py`
- Modify: `backend/app/services/leasing_service.py`

- [ ] **Step 1: 写失败测试（先写测试，跑红）**

```python
"""金租分次放款：多笔、每笔独立还款计划、采购验收前置。"""
import uuid
from datetime import date
from decimal import Decimal

from app.models.acceptance import AcceptanceRecord
from app.models.master import Supplier
from app.models.project import Project
from app.models.user import User
from app.services import leasing_service as svc


def _user(db):
    u = User(username=f"u{uuid.uuid4().hex[:6]}", display_name="t", password_hash="x", role="FINANCE_DIRECTOR", active=True)
    db.add(u); db.flush(); return u


def _project(db):
    p = Project(name="P", code=f"c{uuid.uuid4().hex[:6]}")
    db.add(p); db.flush(); return p


def _lessor(db):
    s = Supplier(name="金租公司", type="资金供应商")
    db.add(s); db.flush(); return s


def _acceptance(db, project_id):
    a = AcceptanceRecord(project_id=project_id, acceptance_type="采购验收", status="已通过")
    db.add(a); db.flush(); return a


def _proc(db, project_id, lessor_id):
    return svc.create_process(db, project_id=project_id, supplier_id=lessor_id,
        total_amount=Decimal("40000000"), annual_rate=Decimal("0.04"),
        term_periods=12, payment_freq="月", repayment_method="等额本息")


def test_add_disbursement_requires_acceptance(db):
    u = _user(db); p = _project(db); l = _lessor(db)
    proc = _proc(db, p.id, l.id)
    from app.core.exceptions import BusinessError
    import pytest
    with pytest.raises(BusinessError):
        svc.add_disbursement(db, process_id=proc.id, amount=Decimal("10000000"),
                             disbursement_date=date(2026, 8, 1), created_by=u.id)


def test_add_disbursement_two_tranches_independent_plans(db):
    u = _user(db); p = _project(db); l = _lessor(db)
    proc = _proc(db, p.id, l.id)
    _acceptance(db, p.id)
    d1, _, n1 = svc.add_disbursement(db, process_id=proc.id, amount=Decimal("10000000"),
                                     disbursement_date=date(2026, 8, 1), created_by=u.id)
    d2, _, n2 = svc.add_disbursement(db, process_id=proc.id, amount=Decimal("5000000"),
                                     disbursement_date=date(2026, 9, 1), created_by=u.id)
    assert n1 == 12 and n2 == 12
    rows = svc.list_disbursements(db, proc.id)
    assert len(rows) == 2
    assert {r.id for r in rows} == {d1.id, d2.id}
    # 每笔还款计划都挂到自己的 disbursement
    from app.models.repayment import Repayment
    from sqlalchemy import select
    r1 = db.execute(select(Repayment).where(Repayment.disbursement_id == d1.id)).scalars().all()
    r2 = db.execute(select(Repayment).where(Repayment.disbursement_id == d2.id)).scalars().all()
    assert len(r1) == 12 and len(r2) == 12
```

- [ ] **Step 2: 跑测试确认失败**

Run: `docker compose exec -T backend pytest app/tests/test_multi_disbursement.py -q`
Expected: FAIL（`add_disbursement` 未定义）

- [ ] **Step 3: 实现 `add_disbursement` + `list_disbursements`**

在 `leasing_service.py` 顶部 import 区加 `from app.models.acceptance import AcceptanceRecord` 和 `from app.models.leasing import LeasingDisbursement`（后者已 import LeasingNode/LeasingProcess，补 LeasingDisbursement）。

在 `disburse()` 函数之后追加：

```python
def add_disbursement(db: Session, *, process_id, amount: Decimal, disbursement_date: date,
                     note: str | None = None, created_by=None) -> tuple:
    """新增一笔放款：前置校验采购验收 → 写放款记录 → 生成该笔还款计划 → 记入金流水 → 资金置换。"""
    proc = db.get(LeasingProcess, process_id)
    if not proc or proc.deleted_at is not None:
        raise BusinessError("NOT_FOUND", "金租申请不存在", 404)
    if proc.status not in ("进行中", "已批", "已放款"):
        raise BusinessError("ILLEGAL_TRANSITION", f"不允许 {proc.status} → 放款", 409)
    if not (proc.annual_rate is not None and proc.term_periods and proc.payment_freq and proc.repayment_method):
        raise BusinessError("BAD_REQUEST", "缺少 利率/期数/频率/方式，无法生成还款计划", 422)
    has_acc = db.execute(select(AcceptanceRecord).where(
        AcceptanceRecord.project_id == proc.project_id,
        AcceptanceRecord.acceptance_type == "采购验收",
        AcceptanceRecord.status == "已通过",
        AcceptanceRecord.deleted_at.is_(None),
    )).first()
    if not has_acc:
        raise BusinessError("PRECONDITION", "需先完成采购验收才能放款", 409)

    d = LeasingDisbursement(process_id=process_id, amount=amount,
                            disbursement_date=disbursement_date, note=note, created_by=created_by)
    db.add(d)
    db.flush()

    rows = generate_plan(principal=amount, annual_rate=proc.annual_rate,
                         term_periods=proc.term_periods, payment_freq=proc.payment_freq,
                         method=proc.repayment_method, disbursement_date=disbursement_date)
    for r in rows:
        db.add(Repayment(leasing_process_id=proc.id, disbursement_id=d.id, period=r.period,
                         due_date=r.due_date, planned_principal=r.planned_principal,
                         planned_interest=r.planned_interest, status="待还"))

    txn = CapitalTransaction(project_id=proc.project_id, source_type="金租融资", direction="IN",
                             amount=amount, transaction_date=disbursement_date,
                             leasing_process_id=proc.id, category="放款",
                             idempotency_key=f"disburse:{d.id}", note=note, created_by=created_by)
    db.add(txn)

    from app.services import funding_service as fs
    fs.execute_replacement(db, project_id=proc.project_id, leasing_process_id=proc.id,
                           disbursement_amount=amount, disbursement_date=disbursement_date,
                           created_by=created_by)

    if proc.status != "已放款":
        proc.status = "已放款"

    from app.services import audit_service as _audit
    _audit.log(db, user_id=created_by, action="DISBURSE", target_type="leasing_disbursement",
               target_id=d.id, after_json={"amount": str(amount), "periods": len(rows)})
    from app.services import workflow_service as _wf
    _wf.after_action(db, proc.project_id)
    return d, txn, len(rows)


def list_disbursements(db: Session, process_id):
    return db.execute(select(LeasingDisbursement).where(
        LeasingDisbursement.process_id == process_id,
        LeasingDisbursement.deleted_at.is_(None),
    ).order_by(LeasingDisbursement.created_at)).scalars().all()
```

- [ ] **Step 4: 跑测试确认通过**

Run: `docker compose exec -T backend pytest app/tests/test_multi_disbursement.py -q`
Expected: PASS（2 passed）

- [ ] **Step 5: 提交**

```bash
git add backend/app/tests/test_multi_disbursement.py backend/app/services/leasing_service.py
git commit -m "feat(leasing): add_disbursement with acceptance precondition + per-tranche repayment"
```

---

## Task 5: 端点 —— POST/GET disbursements

**Files:**
- Modify: `backend/app/api/v1/endpoints/leasing.py`

- [ ] **Step 1: 加导入 + 两个端点**

在 schema import 区加 `DisbursementCreate, DisbursementOut`；在 `disburse` 端点之后追加：

```python
@router.get("/processes/{process_id}/disbursements")
def list_disbursements(process_id: UUID, db: Session = Depends(get_db), user: User = Depends(get_current_user)):
    rows = svc.list_disbursements(db, process_id)
    return {"items": [DisbursementOut.model_validate(r).model_dump(mode="json") for r in rows]}


@router.post("/processes/{process_id}/disbursements", response_model=DisbursementOut, status_code=201)
def add_disbursement(process_id: UUID, payload: DisbursementCreate, db: Session = Depends(get_db),
                     user: User = Depends(get_current_user)):
    try:
        d, _, _ = svc.add_disbursement(db, process_id=process_id, amount=payload.amount,
                                       disbursement_date=payload.disbursement_date,
                                       note=payload.note, created_by=user.id)
        db.commit()
    except IntegrityError:
        db.rollback()
        raise BusinessError("DUPLICATE", "重复放款", 409)
    return DisbursementOut.model_validate(d)
```

- [ ] **Step 2: 提交**

```bash
git add backend/app/api/v1/endpoints/leasing.py
git commit -m "feat(leasing): add disbursement list/create endpoints"
```

---

## Task 6: UI —— 预计放款日 + 放款记录区 + 时间线按钮堆叠修复

**Files:**
- Modify: `frontend/src/views/LeasingView.vue`

- [ ] **Step 1: 新建申请「开始日期」标签改「预计放款日」**

`<n-form-item label="开始日期">` → `<n-form-item label="预计放款日">`（字段名 `start_date` 不动）。

- [ ] **Step 2: 用「放款记录」区替换旧的「放款操作」区**

删除旧 `disburseForm` / `showDisburse` / `doDisburse`（放款表单）及其模板段「放款操作」（第 247–261 行的「实际放款额/放款日/确认放款」）；改为「放款记录」区。在 `nodes` state 附近加：

```ts
const disbursements = ref<any[]>([])
const showAddDisb = ref(false)
const addDisbForm = ref({ amount: null as number | null, disbursement_date: '', note: '' })
```

在 `openDetail` 里并行拉 disbursements：

```ts
const [{ data: d }, { data: r }, { data: ds }] = await Promise.all([
  api.get(`/leasing/processes/${row.id}`),
  api.get('/repayments', { params: { leasing_process_id: row.id } }),
  api.get(`/leasing/processes/${row.id}/disbursements`),
])
disbursements.value = ds.items || []
```

加提交函数：

```ts
async function doAddDisb() {
  const f = addDisbForm.value
  if (!f.amount || !f.disbursement_date) { msg.warning('请填放款额和实际放款日'); return }
  try {
    await api.post(`/leasing/processes/${detail.value.id}/disbursements`, {
      amount: f.amount, disbursement_date: f.disbursement_date, note: f.note || null,
    })
    msg.success('放款已登记，已生成该笔还款计划')
    showAddDisb.value = false
    if (detail.value) await openDetail(detail.value)
  } catch (e: any) { msg.error(errMsg(e)) }
}
```

在模板「9 节点时间线」之后加「放款记录」区（含多笔列表 + 新增按钮 + 新增弹窗），列表每行显示 `放款额 / 实际放款日 / 备注`。

- [ ] **Step 3: 时间线按钮堆叠修复**

`.tl-ops { display: flex; gap: 2px; margin-top: 2px; }` → `.tl-ops { display: flex; flex-direction: column; gap: 2px; margin-top: 2px; }`

- [ ] **Step 4: 前端 type-check + build**

```bash
cd frontend && npm run type-check && npm run build
```

- [ ] **Step 5: 提交**

```bash
git add frontend/src/views/LeasingView.vue
git commit -m "feat(leasing): 预计放款日 + 放款记录区 + 时间线布局修复"
```

---

## Task 7: 收尾验证（pytest + e2e 两轮 0 flake + 构建部署）

**Files:**
- Create: `e2e/tests/leasing-multi-disburse.spec.ts`（分次放款冒烟，端到端铁律）

- [ ] **Step 0: 写分次放款 e2e（端到端铁律）**

`e2e/tests/leasing-multi-disburse.spec.ts`：API 建 项目+资金供应商+采购验收 → 建金租申请 → UI（cfo）在金租详情「放款记录」区点「新增放款」填金额+实际放款日 → 断言放款记录区出现该笔 + 提示生成还款计划。覆盖「多笔放款 + 每笔独立」的前端链路。

- [ ] **Step 1: 全量 pytest**

```bash
docker compose exec -T backend pytest -q
```
Expected: 全绿（含新增 2 条）

- [ ] **Step 2: 重建 + 部署**

```bash
docker compose build backend frontend && docker compose up -d backend frontend
```

- [ ] **Step 3: 全量 e2e 两轮**

```bash
cd e2e && npx playwright test && npx playwright test
```
Expected: 两轮全绿、0 flake

- [ ] **Step 4: 提交 e2e（如有新增）**
